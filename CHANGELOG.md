# Changelog

## 2.1.1

Honesty / copy only. No new features.

- One version string (`2.1.1`) in `package.json`, README, UI, share sheet, and OG cache buster
- Public copy matches the thesis already on the theory landing: color is to light as chord is to sound
- README no longer claims `2.0`; FAQ and “what it is not” match the shipped instrument
- Package name is `colorchord`, not the scaffold leftover `app-builder-workspace`
- About dialog, share text, and meta description no longer disagree with each other
